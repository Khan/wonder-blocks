---
"@khanacademy/wonder-blocks-breadcrumbs": minor
"@khanacademy/wonder-blocks-tabs": minor
---

Breadcrumbs' default `aria-label` and the TabsDropdown and NavigationTabsDropdown default opener label now read translated strings from `WonderBlocksConfigProvider`. The `aria-label` and `labels.defaultOpenerLabel` props override the built-in translations.
