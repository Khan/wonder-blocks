---
"@khanacademy/wonder-blocks-link": minor
---

Link's external-link icon now has an accessible name by default ("Opens in a new window"), and reads a translated one from `WonderBlocksConfigProvider` when it is mounted. `labels.externalIconAriaLabel` still overrides both for a single Link.
