---
"@khanacademy/wonder-blocks-link": minor
---

Link's external-link icon now has an accessible name by default ("(opens in a new tab)"), and reads a translated one from `WonderBlocksI18nContextProvider` when it is mounted. `labels.externalIconAriaLabel` still overrides both for a single Link.
