---
"@khanacademy/wonder-blocks-tokens": major
---

Remove the deprecated legacy `font.size.*` and `font.lineHeight.*` token scales. Use `font.body.*` / `font.heading.*` for typography, or the underlying `sizing.size_*` tokens where a raw value is needed.
