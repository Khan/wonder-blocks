---
"@khanacademy/wonder-blocks-core": minor
---

Adds the translatable strings for the labels and screen reader announcements that Wonder Blocks components currently hardcode. The components still use their own defaults; they will be switched over to `WonderBlocksI18nContextProvider` in a follow up.
