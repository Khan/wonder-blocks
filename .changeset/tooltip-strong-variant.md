---
"@khanacademy/wonder-blocks-tooltip": minor
---

Add a `variant` prop to `Tooltip` to support a new `strong` variant. The `strong` variant renders the tooltip with an inverse/knockout ("black") background and light ("white") text. It defaults to `subtle`, which is the existing tooltip styling.
