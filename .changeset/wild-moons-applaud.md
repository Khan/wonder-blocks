---
"@khanacademy/wonder-blocks-accordion": patch
---

AccordionSection: clip the content panel with `overflow: clip` and, while the section is expanded, an `overflow-clip-margin` that leaves room for a focus ring, so focusable content at the edges of a section (a full width button, for example) no longer has its focus ring cut off
