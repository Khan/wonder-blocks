---
"@khanacademy/wonder-blocks-icon": minor
---

`PhosphorIcon` now automatically mirrors directional icons (arrows, carets, and similar) in RTL via a central whitelist. Use `mirrorInRtl` to opt custom directional SVGs into the same behavior.
