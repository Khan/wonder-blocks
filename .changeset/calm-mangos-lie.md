---
"@khanacademy/wonder-blocks-popover": major
---

Removes `initialFocusDelay`, `viewportPadding` as these are not used in the codebase (or needed by Floating). Replaced `initialFocusId` with `initialFocusRef`.
