node_modules
coverage
coverage-ts
dist
storybook-static
*.tsbuildinfo
utils/**/*.d.ts
!.vscode
.DS_Store
changeset-status.json
# Pnpm errors
pnpm-debug.log
.swc/
.turbo

# If you run `pnpm storybook doctor`, it creates this file, but definitely
# doesn't need to be checked in.
doctor-storybook.log

# Ignore local Claude settings
.claude/settings.local.json

# Generated by `pnpm build:token-docs` via the `prestart:storybook` /
# `prebuild:storybook` hooks. Never checked in.
__docs__/references/
