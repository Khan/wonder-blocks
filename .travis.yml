language: node_js
node_js:
  - stable
cache:
  yarn: true
  directories:
    - node_modules
install:
  - yarn install
  - yarn run build:all
# TODO(kevinb) re-enable after making 'demo' a workspace
#  - cd demo && yarn install && cd ..
  - yarn global add codecov
script:
  - yarn test
  - yarn run coverage && codecov
