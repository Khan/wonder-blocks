import {Meta} from "@storybook/blocks";

<Meta
    title="Packages / Data / Types / GqlFetchOptions<>"
    parameters={{
        chromatic: {
            disableSnapshot: true,
        },
    }}
/>

# GqlFetchOptions\<>

```ts
type GqlFetchOptions<TVariables: {...}, TContext: GqlContext> = {|
    variables?: TVariables,
    context?: Partial<TContext>,
|};
```

This type describes optional configuration to apply when fulfilling a GraphQL request.

See the section on <a href="./?path=/docs/packages-data-graphql--docs">GraphQL</a> for more information.
