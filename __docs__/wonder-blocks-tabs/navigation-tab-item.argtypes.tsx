import {ArgTypes} from "@storybook/react";

export default {
    children: {
        table: {
            type: {
                summary: "React.Element<Link> | (linkProps) => React.Element",
            },
        },
    },
} satisfies ArgTypes;
