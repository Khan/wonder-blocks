/**
 * The data attribute that scopes the theme to a specific element.
 * This is used to apply the theme to a specific element(s) in the DOM.
 */
export const THEME_DATA_ATTRIBUTE = "data-wb-theme";
