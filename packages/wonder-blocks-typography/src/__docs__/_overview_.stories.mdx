import {Meta} from "@storybook/addon-docs";

<Meta
    title="Typography / Overview"
    parameters={{
        chromatic: {
            disableSnapshot: true,
        },
    }}
/>

# Typography

`wonder-blocks-typography` provides a set of standardized components for
displaying text in a consistent way. This includes components for headings,
paragraphs, and text labels.

**Note:** These docs haven't ported yet. We are working on this to get them up
and running as soon as possible.